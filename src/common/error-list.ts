export enum ErrorList {
  // WALLET ERRORS
  existingWallet = 'Wallet already exist',
  walletNotFound = 'Wallet not found',
  // USER ERRORS
  userNotFound = 'User not found',
  existingUser = 'User already existing',
}
